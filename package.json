{
  "name": "stg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "webpack-dev-server --inline --progress --config packages/server/build/webpack.dev.conf.js",
    "start": "npm run dev",
    "lint": "eslint --ext .js,.vue packages/server/src",
    "build": "node packages/server/build/build.js",
    "tsc": "tsc -b",
    "bootstrap": "lerna bootstrap --no-ci --npm-client yarn"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "lerna": "^3.10.7",
    "typescript": "^3.2.4"
  }
}
