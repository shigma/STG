{
  "name": "stg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:bullets": "yarn workspace @stg/bullets build",
    "build:core": "yarn workspace @stg/core build",
    "build:utils": "yarn workspace @stg/utils build",
    "build:doc": "yarn workspace docs build",
    "build": "tsc -b & yarn workspace @stg/core build:sass & yarn workspace web-stg build",
    "serve": "yarn workspace docs dev",
    "lint": "yarn workspace @stg/server lint"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "typescript": "^3.2.4"
  }
}
